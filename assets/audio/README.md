Đặt file nhạc nền hợp pháp (do bạn tự mua/tải có quyền sử dụng) vào đây, đặt tên đúng như trong `js/config.js` (`CONFIG.audio.src`), ví dụ:

```
assets/audio/background-music.mp3
```

Nếu chưa có file, website vẫn chạy bình thường — chỉ là không có nhạc và nút bật/tắt nhạc sẽ tự ẩn.
